name: 'Setup Java'
description: 'Setup Java 17 with Zulu distribution and Gradle'

runs:
  using: 'composite'
  steps:
    - name: Set up Java
      uses: actions/setup-java@v4.5.0
      with:
        java-version: '17'
        distribution: "zulu"
    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v3
